<div class="row">
	<div class="span10">
		<h1>Your Messages</h1>
	</div>
	<div class="span2">
		<%= button_to 'New Message', new_message_path, :method => :get%>
	</div>
</div>

<table class="table table-striped">
 <thead>
  <tr>
    <th class="blue header">件名</th>
    <th class="blue header">イイね！</th>
    <th class="blue header"></th>
  </tr>
 </thead>
 
 <tbody>
<% @messages.each do |message| %>
  <tr>
    <td><%= message.title %></td>
    <!-- <td><%= message.body %></td>-->
    <td><%= message.evaluate %></td>
    <!-- <td><%= link_to 'Show', message %></td>-->
    <td><%= link_to 'Edit', edit_message_path(message) %></td>
    <!-- <td><%= link_to 'Destroy', message, confirm: 'Are you sure?', method: :delete %></td>-->
  </tr>
<% end %>
 </tdoby>
</table>
